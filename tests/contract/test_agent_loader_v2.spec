Spec: docs/hds-agent-loader.md is the v2 loader for shared HDS agent behavior.
Checks:
- the loader points to `.hds/agent-plugin.json`;
- the loader points to `docs/hds-agent-plugin.md` as the canonical two-cycle core;
- the loader requires exactly two sequential cycles;
- the loader forbids adapter-level duplication of the full HDS protocol.
